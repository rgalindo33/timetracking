<% if flash[:notice] %>
  <%= flash[:notice] %>
<% end %>

<h1> <%= @project.name %></h1>
<ul>
  <% @time_entries.each do |time_entry| %>
    <li>
      <p> total time: <%= time_entry.hours %>:<%= time_entry.minutes %></p>
      <p> date:<%= time_entry.date %> </p>
      <p> comments:<%= time_entry.comments %> </p>

      <%= link_to "edit", project_time_entry_edit_path(@project, time_entry) %> -
      <%= link_to "delete", project_time_entry_path(@project, time_entry),
                           method: :delete, data: {confirm: 'are you sure?'} %>
    </li>
  <% end %>
</ul>
